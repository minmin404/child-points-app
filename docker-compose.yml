version: '3.8'

services:
  child-points:
    build: .
    container_name: child-points-app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DB_DIR=/app/data
      - JWT_SECRET=请替换为你自己的密钥
    volumes:
      - ./data:/app/data
    restart: unless-stopped
