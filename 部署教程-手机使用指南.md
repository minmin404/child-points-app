# 📱 积分小当家 — 在自己电脑上部署教程（手机也能用）

> 目标：把"积分小当家"App 运行在你自己的电脑上，这样你的手机连上同一个 WiFi 就能长期使用，不依赖任何临时链接。

---

## 一、先搞清楚：部署后手机怎么访问？

部署后，你的电脑和手机连**同一个 WiFi**（路由器），手机访问一个"局域网地址"就能打开 App，就像访问网页一样。

```
电脑: 192.168.1.100:8080
手机: 访问 http://192.168.1.100:8080
```

**要点**：
- 电脑和手机必须在**同一个 WiFi / 同一个路由器**下
- 电脑**不要关机**、不要断网（关机了手机就连不上了）
- 手机浏览器打开一次后，可"添加到主屏幕"当App用

---

## 二、需要准备什么

| 项目 | 要求 | 说明 |
|------|------|------|
| **一台电脑** | Windows / Mac / Linux 都行 | 就是常开机的这台 |
| **Node.js 22+** | 必须 | 应用运行环境，免费 |
| **手机** | Android / iPhone | 用来访问 |

> 不需要安装数据库！应用自带轻量级 SQLite 数据库，零配置。

---

## 三、第一步：下载项目文件

### 方式A：下载压缩包（推荐）
1. 从我给你的会话里下载 **`child-points-app.zip`**（已打包好，不含测试数据）
2. 解压到电脑上一个目录，例如：
   - Windows: `D:\积分小当家`
   - Mac: `~/积分小当家`

### 方式B：从代码复制
如果项目在云端沙箱，直接把整个 `child-points-app` 文件夹拷到电脑上。

解压后你应该看到这些文件夹/文件：
```
child-points-app/
├── server/          ← 后端代码
├── public/          ← 前端界面
├── Dockerfile       ← (可选)容器部署用
├── docker-compose.yml
├── package.json
└── README.md
```

---

## 四、第二步：安装 Node.js

> 只有没装过 Node.js 才需要做这一步。

### Windows
1. 打开官网：https://nodejs.org/
2. 下载 **LTS 版本**（例如 v22.x LTS），点击下载 `.msi` 安装包
3. 双击安装，一路"下一步"即可（默认选项就行）

### Mac
1. 打开官网：https://nodejs.org/
2. 下载 **LTS 版本**，安装 `.pkg`
3. 或用 Homebrew：`brew install node`

### 验证是否装好
打开终端（Windows 叫"命令提示符"或"PowerShell"，Mac 叫"终端"），输入：
```bash
node -v
```
如果显示类似 `v22.x.x` 就说明装好了 ✅

> ⚠️ **必须 ≥ v22**。如果版本低于22，去官网下载最新 LTS。

---

## 五、第三步：启动应用

### Windows（打开 PowerShell 或命令提示符）
```bash
# 进入项目目录（改成你自己的路径）
cd D:\积分小当家

# 进入 server 目录
cd server

# 安装依赖（第一次需要，要联网）
npm install

# 回到项目根目录
cd ..

# 启动！
node server\index.js
```

### Mac / Linux（打开终端）
```bash
# 进入项目目录
cd ~/积分小当家

# 进入 server 目录
cd server

# 安装依赖（第一次需要，要联网）
npm install

# 回到项目根目录
cd ..

# 启动！
node server/index.js
```

**看到这几行就说明启动成功了**：
```
✔ 已创建默认家长账号: 用户名 parent / 密码 123456
✔ 孩子积分管理系统已启动: http://localhost:8080
   家长账号: parent / 123456
```

> ⚠️ **启动后这个窗口不要关**！关了应用就停了。想停掉就按 `Ctrl + C`。

---

## 六、第四步：找到你电脑的局域网IP

手机要用"局域网IP:8080"访问，需要先知道电脑的IP。

### Windows
```bash
ipconfig
```
在输出里找 **"IPv4 地址"**，类似 `192.168.1.100`、`192.168.0.5` 等。

### Mac / Linux
```bash
ifconfig   # 或 ip addr
```
找 `en0` / `wlan0` 下的 `inet` 地址，类似 `192.168.1.100`。

**记下这个 IP**（例如 `192.168.1.100`）。

---

## 七、第五步：手机访问！

1. 手机连接**同一个 WiFi**（和电脑同一个路由器）
2. 打开手机浏览器（Chrome / Safari / 自带浏览器都行）
3. 地址栏输入：
   ```
   http://你的电脑IP:8080
   ```
   例如：`http://192.168.1.100:8080`
4. 回车就能打开 App 了！🎉

### 添加到主屏幕（像App一样用）
- **iPhone**：浏览器菜单 → "添加到主屏幕"
- **Android**：Chrome 菜单 → "安装应用" 或 "添加到主屏幕"

之后手机上就会出现"积分小当家"图标，点开直接全屏使用。

---

## 八、登录账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 👨‍👩‍👧 家长 | `parent` | `123456` |
| 👦 孩子 | （家长登录后在"孩子"页自己添加） | 自己设 |

**首次使用建议**：
1. 用 `parent / 123456` 登录
2. 点底部 **"孩子"** → **添加孩子**，创建你孩子的账号
3. 之后手机退出，让孩子用自己的账号登录

---

## 九、常见问题排查

| 问题 | 原因 / 解决办法 |
|------|----------------|
| **手机打不开网址** | ① 确认手机和电脑同一WiFi；② 确认电脑窗口没关；③ IP输错了（看第六步）|
| **提示"无法访问此网站"** | 检查是不是把 `localhost` 当地址了，应该用 `电脑IP:8080` |
| **Windows防火墙拦截** | 首次启动时，Windows 弹出防火墙询问，选 **"允许访问"**；或手动在防火墙放行 8080 端口 |
| **端口被占用** | 8080 被占用时，可用 `PORT=8090 node server/index.js` 换个端口（Mac/Linux），Windows 用 `set PORT=8090 && node server\index.js` |
| **npm install 报错** | 确认能联网；Node版本要 ≥22 |
| **重新启动数据还在吗** | 在！数据存在 `data` 文件夹的数据库里，重启不丢。想清空重来，删掉 `data` 文件夹即可 |

---

## 十、（可选）部署到云服务器，手机随时随地访问

如果不想受"同一WiFi"限制，想让手机**随时、任何地方**都能访问，可以部署到云服务器：

### 方式A：Docker 部署
项目已自带 `Dockerfile` 和 `docker-compose.yml`：
```bash
# 在项目目录
docker compose up -d
```

### 方式B：免费云平台
- **Railway / Render / Fly.io** 等平台
- 把项目推到 GitHub，导入平台
- 启动命令填：`node server/index.js`
- 平台会给你一个公网地址，手机随时随地访问

> 这些需要你有云服务账号，步骤略复杂。家庭用的话，**局域网方案（本教程）完全够用**。

---

## 十一、项目文件说明（了解即可）

| 路径 | 作用 |
|------|------|
| `server/index.js` | 后端服务器 + 接口 |
| `server/db.js` | 数据库操作（SQLite） |
| `public/` | 手机端界面（PWA） |
| `data/` | 数据库文件（自动生成，别删） |
| `README.md` | 项目完整说明 |

---

### 🎉 搞定了！

部署好后，全家就可以用手机互相配合了：
- **家长**：发布任务、审核、发积分、扣分
- **孩子**：认领任务、拍照打卡、赚积分

有任何一步卡住，随时告诉我报错内容，我帮你解决。
